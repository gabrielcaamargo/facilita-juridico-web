import { ModalButton } from './ModalButton';
import { ModalRoot } from './ModalRoot';
import { ModalTitle } from './ModalTitle';

export const Modal = {
  Root: ModalRoot,
  Title: ModalTitle,
  Button: ModalButton
};
